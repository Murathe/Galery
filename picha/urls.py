from django.conf import settings
from django.conf.urls.static import static
from . import views

urlpatterns = [
    url(r"^/$", .as_view(), name=""),
    url(r"^/$", .as_view(), name=""),
    url(r"^/$", .as_view(), name=""),
    
]
